{
  "name": "sponsorblock-mobile",
  "version": "1.4.2",
  "private": true,
  "description": "SponsorBlock userscript for m.youtube.com, built from TypeScript modules into a single .user.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.7.0"
  }
}
